version = "1.2.0"
description = "Adds the ability to quickly switch accounts"

aliucord {
    changelog.set(
        """
            # 1.2.0
            Support V105.12
            
            # 1.1.2
            * Fixed 2FA token bug
            
            # 1.0.1
            * Minor bug fixes

            # 1.0.0
            * Initial release, here comes the issues...

            """.trimIndent()
    )
}