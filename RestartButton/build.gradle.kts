version = "1.2.1"
description = "Adds a button to restart Aliucord to the settings page"

aliucord {
    changelog.set(
        """
            
            # 1.2.1
            * Possible fix for restart button not appearing on some devices
                
            # 1.2.0
            * Support V96

        """.trimIndent()
    )
}