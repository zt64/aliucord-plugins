version = "2.1.0"
description = "Adds many options to hide items from the top bar in an attempt to prevent screen burn-in"

aliucord {
    changelog.set(
            """

            # 2.1.0
            * Optimized code and fixed some items that were missed
            
            # 2.0.0
            * Added many new options to change what parts of the top bar are hidden

            """.trimIndent()
    )
}