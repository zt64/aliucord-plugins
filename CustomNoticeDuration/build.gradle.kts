version = "1.2.0"
description = "Allows setting the dismiss time of notices (in-app notifications)"

aliucord {
    changelog.set(
        """
                
            # 1.1.0
            * Support V96

        """.trimIndent()
    )
}