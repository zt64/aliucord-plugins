version = "1.1.0"
description = "Makes long pressing on the emoji button send a random emote"

aliucord {
    changelog.set(
        """
                
            # 1.1.0
            * Support V96

            """.trimIndent()
    )
}