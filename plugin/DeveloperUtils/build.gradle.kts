version = "1.0.0"
description = "Adds several utilities to make development easier"

aliucord {
    excludeFromUpdaterJson.set(true)
    changelog.set(
        """
            
        """.trimIndent()
    )
}