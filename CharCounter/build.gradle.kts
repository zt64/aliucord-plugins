version = "1.1.0"
description = "Adds a character counter to the message box"

aliucord {
    changelog.set(
        """
                
            # 1.1.0
            * Support V96

            """.trimIndent()
    )
}