version = "1.5.0"
description = "Adds a variety of new features to the media viewer"

aliucord {
    changelog.set(
        """
            # 1.5.0
            * Fixed controls not reappearing
                
            # 1.2.0
            * Support V96

        """.trimIndent()
    )
}