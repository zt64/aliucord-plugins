version = "1.2.0"
description = "Shows the total number of banned users on the bans page in the server settings"

aliucord {
    changelog.set(
        """
                
            # 1.1.0
            * Support V96

            """.trimIndent()
    )
}