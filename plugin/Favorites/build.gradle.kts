version = "1.0.0"
description = "Allows you to favorite GIFs and view them later"

aliucord.excludeFromUpdaterJson.set(true)
aliucord.changelog.set(
    """
    # 1.0.1
    * Fixed embeds being wrong width
""".trimIndent()
)