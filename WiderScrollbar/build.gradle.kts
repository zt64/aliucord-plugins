version = "1.2.0"
description = "Allows changing the scrollbar width to make it easier to drag"

aliucord {
    changelog.set(
        """
                
            # 1.2.0
            * Support V96

        """.trimIndent()
    )
}