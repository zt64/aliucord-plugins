version = "1.7.2"
description = "Allows making server icons and user avatars always animate"

aliucord {
    changelog.set(
        """
            # 1.6.1
            * Fixed plugin not working
            
            # 1.6.0
            * Support V96

        """.trimIndent()
    )
}