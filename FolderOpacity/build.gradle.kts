version = "1.3.0"
description = "Adds an option to the guild folder settings to set the opacity"

aliucord {
    changelog.set(
            """
                
            # 1.3.0
            * Support V96

            """.trimIndent()
    )
}